bundle_path = "../bundle.h5"
cosmology_path = "../cosmology.toml"
detectors = ["E1", "E2", "E3"]
sample_only = ["H0", "w0"]
seed = 1

[model]
# Cosmology model: "LambdaCDM" (default), "W0CDM", or "W0WaCDM".
cosmology = "W0CDM"

# Optional. Relative paths are resolved against this TOML file's directory.
output_dir = "../chains"
output_prefix = "chains"

[init]
H0 = 67.66
"Ωm" = 0.3096
w0 = -1.0
wa = 0.0
"Ξ₀" = 1.0
"Ξₙ" = 1.91
"γ" = 2.7
"κ" = 5.7
zpeak = 2.0

[sampler]
# Keep these small for smoke tests; increase for real runs.
n_samples = 3000
n_adapts = 3000
target_acceptance = 0.9
# num_chains defaults to Base.Threads.nthreads().
# checkpoint_every = 0 disables intra-run checkpointing.
num_chains = 8
checkpoint_every = 500
